use @std:io/{ log };

let fibonacci: [int] = [1, 2, 3, 5, 8];
fibonacci.push(13);
for (mut i = 0; i < fibonacci.length; i = i + 1) {
  log(fibonacci[i].toString());
}

mut can-reassign: [int] = [1];
can-reassign[0] = 100;
log(can-reassign[0].toString());
