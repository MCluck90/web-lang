use @std:io/{ log };

// TODO: Add support for property access after function calls
let logInt(n: int) { log(n.toString()) }


let createAdder(x: int): (int) -> int {
  let adder(y: int): int { x + y }
  adder
}

let add10 = createAdder(10);

// 25
logInt(add10(15));

// 100
logInt(add10(90));