use @std:io/{ log };


let create-adder(x: int): (int) -> int {
  let adder(y: int): int { x + y }
  adder
}

let add10 = create-adder(10);

// 25
log(add10(15).toString());

// 100
log(add10(90).toString());
